'use strict';

/**
 * @ngdoc function
 * @name impala2GoApp.controller:RedimCtrl
 * @description
 * # RedimCtrl
 * Controller of the impala2GoApp
 */
angular.module('impala2GoApp')
  .controller('RedimCtrl', function ($scope,logger) {
        var log= logger.getLogFn("RedimCtrl");
        log("redim controller","",true);
  });
